import { jsx } from '@emotion/core';

import { Theme } from 'models/Theme';

const theme: Theme = {
	color: {
		black: '#1f1f1f',
		blue: '#007bff',
		gray: '#f8f8f8',
		white: '#fff',
	},
};

export { jsx, theme };
